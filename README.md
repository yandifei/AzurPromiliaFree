# AzurPromiliaFree
building......
yolo模型数据采集+炼丹ing

有啥建议issues提吧
UI是让我最头痛的，PyQt6框架(非魔改版，原生UI烂)我熟，但是想上Electron框架作为前端(快速迭代+炫彩UI)
用FastA使PI做为后端交互接口（NoneBot、AstrBot、OpenClaw等机器人也能通过接口去指令完成任务）
yolo确实可以做到无视分辨率去点击屏幕(具体实现是后台或前台方式)，但是没有仔细研究过相关的demo，比如click(0,1)还是click(0%,1%)?
截图就DXGI，模型推理多种选择，问题：cpu用onnx模型，但是pt模型可以转其他模型格式，希望A卡N卡用户都能用上显卡加速推理。
